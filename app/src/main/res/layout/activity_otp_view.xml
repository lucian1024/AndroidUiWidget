<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".OtpViewActivity">
        <com.lucian.otpview.OtpView
            android:id="@+id/otpview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginTop="20dp"
            app:edit_width="50dp"
            app:edit_height="50dp"
            app:edit_inputType="number"
            app:edit_num="5"
            app:edit_textSize="14sp"
            app:edit_textColor="@android:color/black"
            app:edit_normalBg="@drawable/otpview_normal"
            app:edit_successBg="@drawable/otpview_success"
            app:edit_errorBg="@drawable/otpview_error"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:layout_marginStart="10dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="10dp"
            android:text="@string/otpview_info"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/otpview" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>